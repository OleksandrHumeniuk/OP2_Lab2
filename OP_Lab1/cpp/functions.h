#pragma once

#include <iostream>
#include <fstream>
#include <string>
#include <vector>

using namespace std;

int chooseMode();
void createInputFile(string, int);
void createOutputFile(string, string);
void printFile(string, string);